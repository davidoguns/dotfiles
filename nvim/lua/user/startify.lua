---@diagnostic disable-next-line: unused-local
local status_ok, startify = pcall(require, "startify")
if not status_ok then
  return
end
-- leave this plugin in it's default configured start screen
-- may revisit later

